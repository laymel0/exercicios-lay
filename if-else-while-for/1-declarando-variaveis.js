/**
 * 1. Crie variáveis para armazenar:
 *      - Seu nome (tipo string)
 *      - Sua idade (tipo number)
 *      - Se você é estudante (tipo boolean)
 * 
 * Use pelo menos uma de cada (var, let e const)
 */


// Maneiras de incrementar um número;
let numero = 10;
numero++; // Adiciona mais um número
numero = numero + 4; // Incrementa um número desejado
numero += 8; // Incrementa um número desejado

console.log(numero);
